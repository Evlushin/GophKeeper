# GophKeeper

> Клиент-серверная система для надёжного и безопасного хранения приватной информации: логинов, паролей, бинарных данных, банковских карт и других секретов.

## 📋 Оглавление

- [Описание](#-описание)
- [Требования](#-требования)
- [Установка и запуск](#-установка-и-запуск)
- [Использование](#-использование)
    - [Глобальные флаги](#глобальные-флаги)
    - [Основные команды](#основные-команды)
    - [Работа с секретами](#работа-с-секретами)
- [Типы данных](#-типы-данных)
- [Примеры](#-примеры)
- [Обработка ошибок](#-обработка-ошибок)
- [Структура проекта](#-структура-проекта)
- [Лицензия](#-лицензия)

---

## 🔍 Описание

**GophKeeper** — это защищённое хранилище конфиденциальных данных с клиент-серверной архитектурой. Приложение позволяет:

- ✅ Регистрироваться и авторизоваться в системе
- 🔐 Хранить логины и пароли
- 💳 Сохранять данные банковских карт
- 📁 Загружать и скачивать бинарные файлы
- 🔄 Обновлять и удалять записи
- 🛡️ Использовать TLS-шифрование для передачи данных

---

## ⚙️ Требования

- Go 1.21+
- Доступ к серверу GophKeeper (по умолчанию `https://localhost:8080`)
- TLS-сертификат (для безопасного соединения)

---

## 🚀 Установка и запуск

```bash
# Перейдите в директорию клиента
cd cmd/client

# Запустите клиент
go run main.go
```

> 💡 Для сборки исполняемого файла:
> ```bash
> go build -o gophkeeper main.go
> ```

---

## 📖 Использование

### Глобальные флаги

| Флаг | Краткий | Описание | Значение по умолчанию |
|------|---------|----------|----------------------|
| `--address` | `-a` | Адрес сервера | `https://localhost:8080` |
| `--cert` | `-c` | Публичный сертификат | `../../cert/cert.pem` |
| `--dir-file` | `-s` | Папка для хранения файлов | `./file` |
| `--storage-file` | `-f` | Файл локального хранилища | `storage.txt` |
| `--level` | — | Уровень логирования | `info` |
| `--help` | `-h` | Справка по команде | — |

### Основные команды

```bash
# Регистрация нового пользователя
go run main.go register -l <login> -p <password>

# Авторизация
go run main.go login -l <login> -p <password>

# Выход из системы
go run main.go logout

# Получение справки
go run main.go help
go run main.go <command> --help
```

### Работа с секретами

Команда `secret` требует предварительной авторизации.

#### Подкоманды

| Команда | Описание |
|---------|----------|
| `add` | Добавить новые данные |
| `list` | Посмотреть список секретов |
| `show` | Просмотреть конкретный секрет |
| `update` | Обновить существующие данные |
| `remove` | Удалить секрет |

#### Флаги для `secret add`

| Флаг | Описание | Применимость |
|------|----------|-------------|
| `--type`, `-t` | Тип секрета: `login_password`, `binary`, `card` | **Обязательный** |
| `--title` | Название секрета | Все типы |
| `--login`, `-l` | Логин | `login_password` |
| `--pass`, `-p` | Пароль | `login_password` |
| `--data`, `-d` | Текстовые данные | `login_password`, `card` |
| `--file` | Путь к файлу | `binary` |
| `--chunk_size` | Размер чанка для загрузки больших файлов | `binary` |
| `--number` | Номер карты | `card` |
| `--holder` | Владелец карты | `card` |
| `--expiry` | Срок действия карты | `card` |
| `--cvv` | CVV-код | `card` |
| `--bank` | Название банка | `card` |
| `--metadata`, `-m` | Дополнительные метаданные (JSON) | Все типы |

---

## 🗂️ Типы данных

### `login_password`
Хранение учётных данных:
```bash
go run main.go secret add \
  --type login_password \
  --title "GitHub" \
  --login "user@example.com" \
  --pass "super_secret_pass" \
  --metadata '{"url":"https://github.com"}'
```

### `binary`
Загрузка файлов:
```bash
# Загрузка файла
go run main.go secret add \
  --type binary \
  --title "Документ" \
  --file "./docs/secret.pdf" \
  --chunk_size "1048576"
  
echo "privet kak dela" | go run main.go secret add --type binary

# Просмотр списка бинарных данных
go run main.go secret list --type binary
```

### `card`
Данные банковской карты:
```bash
go run main.go secret add \
  --type card \
  --title "Основная карта" \
  --number "4111111111111111" \
  --holder "IVAN IVANOV" \
  --expiry "12/25" \
  --cvv "123" \
  --bank "Tinkoff"
```

> ⚠️ **Внимание**: Никогда не передавайте чувствительные данные через аргументы командной строки в производственной среде — используйте интерактивный ввод или защищённые переменные окружения.

---

## 💡 Примеры

```bash
# Регистрация и вход
go run main.go register -l myuser -p mypass
go run main.go login -l myuser -p mypass

# Добавление пароля (интерактивный ввод пароля)
go run main.go secret add -t login_password --title "Email" -l user@mail.ru -p

# Просмотр всех секретов
go run main.go secret list

# Фильтрация по типу
go run main.go secret list --type card

# Обновление секрета
go run main.go secret update --id <secret_id> --pass "new_password"

# Удаление секрета
go run main.go secret remove --id <secret_id>
```

---

## ❌ Обработка ошибок

| Сообщение | Причина | Решение |
|-----------|---------|---------|
| `unknown flag: --ff` | Неверный флаг команды | Проверьте доступные флаги через `--help` |
| `ошибка получения секрета: тип данных не найден` | Неверный `--type` | Используйте: `login_password`, `binary`, `card` |
| `unauthorized` | Требуется авторизация | Выполните `login` перед работой с секретами |
| `connection refused` | Сервер недоступен | Проверьте адрес сервера и запущен ли он |

---

## 🔐 Безопасность

- Все данные передаются по TLS
- Пароли не отображаются при вводе (используйте `-p` без значения для интерактивного режима)
- Локальное хранилище может быть зашифровано (настраивается)

---

## 📄 Лицензия

Распространяется под лицензией MIT. Подробнее — в файле [LICENSE](LICENSE).

---

> 🛠 **Разработано с ❤️ на Go**  
> Если вы нашли баг или есть предложение — создайте Issue в репозитории.